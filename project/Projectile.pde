class Projectile {

  int gameScore;
  int collisionScore;
  int xSpeed;
  int ySpeed;

  int numRows = NUM_ROWS;
  int numCols = NUM_COLS;
  int size = ballSize;
  color ballColor = colorArray[difficulty];

  void Projectile () {
  }

  void collisionCheck() {
  }
}
